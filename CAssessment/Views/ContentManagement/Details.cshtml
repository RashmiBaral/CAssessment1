@model IEnumerable<ModelsAndUtility.Model.CaseStudyDetails>

@{
    ViewData["Title"] = "Details";
    Layout = "~/Views/Shared/_CAUtilityLayout.cshtml";
}

<h1>Details</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CSID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CaseStudy)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AssessmentID)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ReviewStatus)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CreatedBy)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.CSID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CaseStudy)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.AssessmentID)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.ReviewStatus)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.CreatedBy)
                </td>
                <td>
                    @Html.ActionLink("Edit", "Edit", new { id = item.CSID }) |
                    @Html.ActionLink("Details", "Edit", new {  id=item.CSID }) |
                    @*@Html.ActionLink("Delete", "Delete", new { /* id=item.PrimaryKey */ })*@
                </td>
            </tr>
        }
    </tbody>
</table>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}